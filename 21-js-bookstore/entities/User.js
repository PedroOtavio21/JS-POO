module.exports = class User{
    // Classe destinada ao usuário (cliente)
    constructor(name, email, password){
        this.name = name
        this.email = email
        this.password = password
    }
}